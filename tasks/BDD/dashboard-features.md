# BDD Tasks: Dashboard Features

## Objective
Define behavior-driven development (BDD) scenarios for the expanded pet dashboard features.

## Tasks
- [ ] Add Gherkin scenarios to `BDD.md` for viewing Pet Status (Hunger, Energy, Happiness).
- [ ] Add Gherkin scenarios for accessing the Inventory from the dashboard.
- [ ] Update existing Gherkin scenarios to reflect routing to `/games` instead of `/game`.
- [ ] Add Gherkin scenarios for viewing the User Profile Summary (Rank/Level).

## Notes
Ensure these scenarios align with the current user login flow.
