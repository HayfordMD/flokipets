# Testing Tasks: Dashboard E2E Tests

## Objective
Write End-to-End (E2E) tests for the expanded dashboard features.

## Tasks
- [ ] Write tests validating that the dashboard correctly renders the pet state (Hunger, Energy, Happiness) when data is present.
- [ ] Write tests verifying that clicking each button in the navigation grid routes to the correct screen:
  - Shop (`/shop`)
  - Games (`/games`)
  - Marketplace (`/marketplace`)
  - Friends (`/friends`)
  - Bank (`/bank`)
  - Inventory (`/inventory`)

## Dependencies
- Requires Frontend tasks to be completed so the UI is testable.
